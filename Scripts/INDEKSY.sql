create index productIndex on products(coffeename);
create index employeeIndex on employees(employee_id)

-- tworzy strukture drzewa binearnego opartą na wskazanych polach, 
-- która potrafi przyspieszyć poruszanie się po danych ze złożoności O(n) do O(ln(n))